programa
   a, b, soma: inteiro;

   funcao somador(x: inteiro, y: inteiro) : inteiro
   r: inteiro;
   inicio
      r := x + y;
      retorne r;
   fim ;

inicio
   a := "ds";
   b := 3;
   soma := somador(a, b);
   saida a, b, soma;
fim;
