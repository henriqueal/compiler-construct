programa
   a, b, soma: inteiro;

   funcao somador(x: inteiro, y: inteiro) : inteiro
   r: inteiro;
   inicio
      r := x + y;
      retorne r;
   fim ;

inicio
   a := 2;
   b := 3;
   soma := somador(a, b) - 1;
   saida 2*a - 3, a, b, soma, 5;
fim;